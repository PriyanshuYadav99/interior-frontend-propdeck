VITE_API_URL=https://interior-backend-production.up.railway.app

# Optional — if unset, src/config/env.js falls back to the previously
# hardcoded key so behavior is unchanged.
# VITE_GOOGLE_MAPS_API_KEY=your_key_here
